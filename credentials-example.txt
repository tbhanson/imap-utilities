;; Example ~/.imap_secrets/credentials file showing both password and OAuth2 accounts.
;;
;; For OAuth2 (Gmail) accounts:
;;   - hostname is "imap.gmail.com"
;;   - password is "" (not used — OAuth2 token is used instead)
;;   - xoauth2? is #t
;;
;; For password accounts:
;;   - xoauth2? is #f

(
  ;; Password-authenticated accounts
  #s(imap-email-account-credentials "my-other-mail" "imap.example.com" "me@example.com" "mypassword" #f #f)

  ;; Gmail accounts (OAuth2)
  #s(imap-email-account-credentials "my-gmail" "imap.gmail.com" "me@gmail.com" "" #f #t)
  #s(imap-email-account-credentials "my-gmail-2" "imap.gmail.com" "other@gmail.com" "" #f #t)
)
